[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "ADMIN_EMAILS"

# SPA fallback (keep this only)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200